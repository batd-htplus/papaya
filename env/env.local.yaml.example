# TEMPLATE for env/env.local.yaml — copy this file, then edit the copy:
#
#   cp env/env.local.yaml.example env/env.local.yaml
#
# env/env.local.yaml is GITIGNORED. Put secrets and machine-local overrides
# HERE ONLY (never in the committed env/env.yaml). Values here override
# env/env.yaml. This .example is committed, so it must hold NO real secrets.

username: <your-username>
password: <your-password>

# Optional local overrides (uncomment as needed):
# base_url: http://localhost:3000
# headless: false
